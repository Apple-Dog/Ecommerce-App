const OrderStatus = Object.freeze({
    ORDERED : "ORDERED",
    SHIPPED : "SHIPPED",
    DELIVERED : "DELIVERED",
    CANCELLED : "CANCELLED",
});

export default OrderStatus;

// Enum Created For Order Status
