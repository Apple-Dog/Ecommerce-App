const PaymentStatus = Object.freeze({
    CREATED : "created",
    AUTHORIZED : "authorized",
    CAPTURED : "captured",
    REFUNDED : "refunded",
    FAILED : "failed",
});

export default PaymentStatus;

// Enum Created For Payment Status